<?xml version="1.0" encoding="utf-8"?>
<!-- Layout del header del Navigation Drawer -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="200dp">

    <!-- Imagen de fondo del header -->
    <ImageView
        android:id="@+id/header_bg"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:src="@drawable/headerphoto" />

    <!-- Contenedor vertical para la foto de perfil y el email/Spinner -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_centerInParent="true"
        android:gravity="center_horizontal">

        <!-- Imagen de perfil circular -->
        <ImageView
            android:id="@+id/profile_image"
            android:layout_width="72dp"
            android:layout_height="72dp"
            android:src="@drawable/fotocuadrada"
            android:scaleType="centerCrop"
            android:background="@drawable/circle_shape"
            android:padding="2dp" />

        <!-- Spinner de emails debajo de la foto de perfil -->
        <Spinner
            android:id="@+id/email_spinner"
            android:layout_width="wrap_content"
            android:layout_height="48dp"
            android:layout_marginTop="12dp"
            android:spinnerMode="dropdown"/>
    </LinearLayout>
</RelativeLayout>
